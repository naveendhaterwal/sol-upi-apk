[workspace]
members = [
    "backend",
]

[package]
name = "solupi-root"
version = "0.1.0"
edition = "2021"

[dependencies]
solupi-backend = { path = "./backend" }
vercel_runtime = "1.1.0"
tokio = { version = "1", features = ["full"] }
axum = "0.7"
